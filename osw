// 节点接口
export interface Node {
  id: string; // 唯一标识
  name: string; // 节点名称
  // 可扩展其他属性（如颜色、大小）
}

// 关系接口（带方向）
export interface Link {
  source: string; // 源节点id
  target: string; // 目标节点id
  label?: string; // 关系描述（可选）
}
